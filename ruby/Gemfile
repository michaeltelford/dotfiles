source 'https://rubygems.org' do
  gem 'rake'
  gem 'rack'
  gem 'rails', '>=5.0.0'
  gem 'hanami'
  gem 'sinatra'
  gem 'thin'
  gem 'json'
  gem 'http'

  group :development do
    gem 'rb-readline'
    gem 'pry-coolline'
	  gem 'pry'
	  gem 'byebug'
    gem 'hijack'
    gem 'guard'
	end

  # My own personally developed gems.
  group :personal do
    gem 'wgit'
  end
  
  # Place any problematic install gems at the end. 
  gem 'nokogiri' # `xcode-select --install` should be complete on mac os x. 
end
